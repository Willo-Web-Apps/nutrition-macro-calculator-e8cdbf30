---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const { title = "Somatix — Tu composición corporal, convertida en un plan de alimentación", description = "Ingresa tu grasa corporal, grasa visceral y altura. Obtén tus macros exactos para volumen, definición o mantenimiento en segundos. Gratis, sin registro." } = Astro.props;

// Canonical URL — drives <link rel="canonical">, og:url, sitemap, JSON-LD.
// Astro.site is set in astro.config.mjs (`site:` field). If unset, this
// throws at build time, which is the desired loud failure rather than
// silently emitting broken canonical markup.
const canonicalUrl = new URL(Astro.url.pathname, Astro.site).href;
const siteUrl = Astro.site?.href ?? '';

// Escape '<' as '\u003c' so a business name/description containing
// "</script>" cannot break out of the JSON-LD <script> tag and execute
// arbitrary JS — defense against stored XSS via tenant-controlled fields.
const orgSchema = JSON.stringify({
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "Somatix",
  "description": "Calculadora de macros y nutrientes basada en composición corporal real: grasa corporal, grasa visceral y altura. Planes de volumen, definición y mantenimiento.",
  "url": siteUrl,
}).replace(/</g, '\\u003c');
---
<!doctype html>
<html lang="es" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <title>{title}</title>
    <script type="application/ld+json" set:html={orgSchema}></script>
  </head>
  <body class="min-h-screen flex flex-col bg-surface-50 text-surface-950 font-body antialiased">
    <Header />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />
  </body>
</html>
